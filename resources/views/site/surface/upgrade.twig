<upgrade url="{{ route('api_upgrade', '__grid__') }}"
         store-url="{{ route('api_upgrade_store', '__grid__') }}"
         destroy-url="{{ route('api_upgrade_destroy', '__grid__') }}" inline-template>
    <div class="upgrade modal fade" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">
                        &times;
                    </button>
                    <h4 class="modal-title">
                        {{ vue('building.name') }}
                    </h4>
                </div>
                <div class="modal-body">
                    {% include 'site.surface.building' %}
                </div>

                {% include 'site.surface.scout' %}
                {% include 'site.surface.trader' %}
                {% include 'site.surface.training' %}
                {% include 'site.surface.transmute' %}
            </div>
        </div>
    </div>
</upgrade>
