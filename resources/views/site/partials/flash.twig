{% if session_has('_flash_manager') %}
    <script type="text/javascript">
        swal({
            title: '{{ session('_flash_manager.title') }}',
            text: '{{ session('_flash_manager.message') }}',
            type: '{{ session('_flash_manager.type') }}',
            showConfirmButton: false,
            timer: 1500
        }).catch(swal.noop);
    </script>
{% elseif session_has('status') %}
    <script type="text/javascript">
        swal({
            title: '{{ trans('messages.success.singular') }}',
            text: '{{ session('status') }}',
            type: 'success',
            showConfirmButton: false,
            timer: 1500
        }).catch(swal.noop);
    </script>
{% elseif errors is not empty %}
    <script type="text/javascript">
        swal({
            title: '{{ trans('messages.error.whoops') }}',
            text: '{{ trans('messages.error.wrong') }}',
            type: 'error',
            showConfirmButton: false,
            timer: 1500
        }).catch(swal.noop);
    </script>
{% endif %}
