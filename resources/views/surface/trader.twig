<trader :type="{{ constant('Koodilab\\Models\\Building::TYPE_TRADER') }}"
        :building="building"
        :grid="grid"
        :close="close"
        url="{{ route('api_planet_capital') }}"
        store-url="{{ route('api_movement_trade_store', '__planet__') }}"
        :unit-types="{{ {
            'transporter': constant('Koodilab\\Models\\Unit::TYPE_TRANSPORTER')
        }|json_encode }}" inline-template>
    <div v-if="isEnabled" class="trader">
        {% include 'partials.cargo' %}
    </div>
</trader>
